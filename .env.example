HOST=localhost
PORT=8001

# --- Database ---
# Matches the credentials in docker-compose.yml.
# Run `docker compose up -d` then `yarn prisma:deploy` to apply migrations.
DATABASE_URL="postgresql://fastify:fastify@localhost:5440/fastify_dev?schema=public"